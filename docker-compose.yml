services:
  app:
    build: .
    container_name: toughskill-app
    restart: unless-stopped
    user: '${APP_UID:-1000}:${APP_GID:-1000}'
    env_file: .env
    volumes:
      - ./data:/data
    networks:
      - default
      - web

networks:
  web:
    external: true
