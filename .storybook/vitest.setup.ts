// Since Storybook 10.3, @storybook/addon-vitest applies preview annotations
// automatically — including @storybook/addon-a11y's axe integration. Leave
// this file empty (but present) so the Vitest project config keeps working;
// add custom test-only setup here if needed.
export {};
